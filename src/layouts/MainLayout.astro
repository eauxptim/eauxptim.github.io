---
import "../styles/global.css";

const {
  title = "Eauxptim",
  description = "Solutions for water and wastewater process optimization.",
} = Astro.props;

const base = import.meta.env.BASE_URL;
const link = (path = "") => `${base}${path}`;

const nav = [
  { label: "Home", href: link("") },
  { label: "About", href: link("about/") },
  { label: "Services", href: link("services/") },
  { label: "Contact", href: link("contact/") },
];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href={link("favicon.svg")} />
    <title>{title}</title>
  </head>
  <body>
    <header class="site-header">
      <div class="container header-inner">
        <a class="brand" href={link("")}>
          <img class="brand-logo" src={link("favicon_2.svg")} alt="Eauxptim logo" />
          <span class="brand-text">Eauxptim</span>
        </a>

        <nav class="site-nav" aria-label="Main navigation">
          {
            nav.map((item) => (
              <a href={item.href}>{item.label}</a>
            ))
          }
        </nav>
      </div>
    </header>

    <main>
      <slot />
    </main>

    <footer class="site-footer">
      <div class="container footer-grid">
        <div>
          <h3>Eauxptim</h3>
          <p>
            Research, software, and engineering for water and wastewater process
            optimization.
          </p>
        </div>

        <div>
          <h4>Navigation</h4>
          <ul>
            {
              nav.map((item) => (
                <li><a href={item.href}>{item.label}</a></li>
              ))
            }
          </ul>
        </div>

        <div>
          <h4>Contact</h4>
          <ul>
            <li><a href="mailto:eauxptim@gmail.com">eauxptim@gmail.com</a></li>
            <li>Belgium</li>
            <li><a href="tel:+32494827212">+32 494 82 72 12</a></li>
          </ul>
        </div>
      </div>

      <div class="container footer-bottom">
        <p>© {new Date().getFullYear()} Eauxptim. All rights reserved.</p>
      </div>
    </footer>
  </body>
</html>